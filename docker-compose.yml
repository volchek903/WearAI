version: "3.9"

services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/wearai.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
